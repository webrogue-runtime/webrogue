name: webrogue artifacts

on:
  workflow_dispatch:

jobs:
  windows:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Submodules
      run: |
        git submodule update --init --recursive --single-branch external/wasmer/;
        git submodule update --init --recursive --single-branch external/xz/;
        git submodule update --init --recursive --single-branch external/dirent/;
        git submodule update --init --recursive --single-branch external/pdcurses/;
        git submodule update --init --recursive --single-branch external/libuv/;
        git submodule update --init --recursive --single-branch external/uvwasi/;
        
    - uses: ilammy/msvc-dev-cmd@v1
    
    - name: Install packages
      run: |
        python3 -m pip install zstd
        
    - name: Build
      run: |
        ./platforms/Windows/build.ps1 Release;

    - uses: actions/upload-artifact@v4
      with:
        name: Artifacts-Windows-MSIX
        path: ./webrogue.msix

    - uses: actions/upload-artifact@v4
      with:
        name: Artifacts-Windows-Installer
        path: ./artifacts/webrogue_installer.exe

  web:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Submodules
      run: |
        git submodule update --init --recursive --single-branch \
          external/xz/ \
          external/libuv/ \
          external/uvwasi/

    - name: Install Emscripten
      run: |
        git clone https://github.com/emscripten-core/emsdk.git
        ./emsdk/emsdk install latest
        ./emsdk/emsdk activate latest
    
    - name: Install packages
      run: |
        sudo apt-get install python3-zstd

    - name: Setup Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.3'

    - name: Configure and build
      run: |        
        . ./emsdk/emsdk_env.sh
        emcmake cmake -B platforms/Web/build -S platforms/Web -DCMAKE_BUILD_TYPE=Release
        cmake --build platforms/Web/build --target pack_artifacts -j

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: Artifacts-Web
        path: |
          ./artifacts
      
  linux:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Submodules
      run: |
        git submodule update --init --recursive --single-branch external/wasmer/
        git submodule update --init --recursive --single-branch external/xz/
        git submodule update --init --recursive --single-branch external/argparse/
        git submodule update --init --recursive --single-branch external/libuv/
        git submodule update --init --recursive --single-branch external/uvwasi/
        git submodule update --init --recursive --single-branch external/SDL/
        git submodule update --init --recursive --single-branch external/SDL_ttf/

    - name: Install packages
      run: |
        sudo apt-get install python3-zstd

    - name: Build RPM and DEB packages
      run: |
        sh platforms/Linux/ubuntu.sh
        sh platforms/Linux/deb2appimage.sh
        mkdir artifacts
        mv platforms/Linux/webrogue.deb platforms/Linux/webrogue.rpm platforms/Linux/Webrogue-x86_64.AppImage artifacts

    - uses: actions/upload-artifact@v4
      with:
        name: Artifacts-Linux
        path: |
          ./artifacts

  dos:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install packages
      run: |
        sudo apt-get install -y libfl2
        sudo apt-get install python3-zstd

    - name: Submodules
      run: |
        git submodule update --init --recursive --single-branch external/xz/
        git submodule update --init --recursive --single-branch external/argparse/
        git submodule update --init --recursive --single-branch external/pdcurses/
        git submodule update --init --recursive --single-branch external/libuv/
        git submodule update --init --recursive --single-branch external/uvwasi/

    - name: Install DJGPP toolchain
      run: |
        wget https://github.com/andrewwutw/build-djgpp/releases/download/v3.4/djgpp-linux64-gcc1220.tar.bz2
        sudo tar -xjvf djgpp-linux64-gcc1220.tar.bz2 -C /usr/local/

    - name: Configure and build
      run: |
        cmake --toolchain=djgpp_toolchain.cmake -S platforms/DOS/ -B platforms/DOS/build -DCMAKE_BUILD_TYPE=Release
        cmake --build platforms/DOS/build --target pack_executable_to_artifacts -j

    - uses: actions/upload-artifact@v4
      with:
        name: Artifacts-DOS
        path: |
          ./artifacts

  android:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin' # See 'Supported distributions' for available options
          java-version: '17'

      - name: Install packages
        run: |
          sudo apt-get install python3-zstd

      - name: Submodules
        run: |
          git submodule update --init --recursive --single-branch external/wamr/
          git submodule update --init --recursive --single-branch external/wasmer/
          git submodule update --init --recursive --single-branch external/xz/
          git submodule update --init --recursive --single-branch external/SDL/
          git submodule update --init --recursive --single-branch external/SDL_ttf/
          git submodule update --init --recursive --single-branch external/libuv/
          git submodule update --init --recursive --single-branch external/uvwasi/

      - name: Install rust targets
        run: |
          rustup target add aarch64-linux-android
          rustup target add x86_64-linux-android

      - name: Build
        run: |
          cd platforms/Android
          ORG_GRADLE_PROJECT_RELEASE_STORE_PASSWORD=${{ secrets.ANDROID_STORE_PASSWORD }} ORG_GRADLE_PROJECT_RELEASE_KEY_PASSWORD=${{ secrets.ANDROID_KEY_PASSWORD }} ./gradlew --no-daemon assembleRelease
          cd ../..
          rm -rf artifacts
          mkdir artifacts
          cp ./platforms/Android/app/build/outputs/apk/release/app-release.apk artifacts/webrogue.apk

      - uses: actions/upload-artifact@v4
        with:
          name: Artifacts-Android
          path: artifacts/webrogue.apk

  macos:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4

    - name: Submodules
      run: |
        git submodule update --init --recursive --single-branch external/xz/
        git submodule update --init --recursive --single-branch external/libuv/
        git submodule update --init --recursive --single-branch external/uvwasi/
        git submodule update --init --recursive --single-branch external/wasmer/
        git submodule update --init --recursive --single-branch external/SDL/
        git submodule update --init --recursive --single-branch external/SDL_ttf/

    - name: Install packages
      run: |
        python3 -m venv .venv
        . .venv/bin/activate
        python3 -m pip install zstd

    - name: Install rust tools
      run: |
        rustup target add aarch64-apple-darwin x86_64-apple-darwin
        cargo install cargo-lipo

    - name: Cmake Cache
      uses: actions/cache@v4
      with:
        path: platforms/MacOS/cmake_build/CMakeCache.txt
        key: macos-cmake-cache

    - name: Configure and build
      run: |
        . .venv/bin/activate
        sh platforms/MacOS/build.sh --release --universal

    - name: Pack to DMG
      run: |
        npm install --global create-dmg
        create-dmg artifacts/webrogue.app artifacts --dmg-title=webrogue || true
        mv artifacts/webrogue\ 0.1.dmg webrogue.dmg

    - uses: actions/upload-artifact@v4
      with:
        name: Artifacts-MacOS
        path: |
          webrogue.dmg

  ios:
    runs-on: macos-latest
        
    steps:
      - uses: actions/checkout@v4
      
      - name: Submodules
        run: |
          git submodule update --init --recursive --single-branch external/xz/
          git submodule update --init --recursive --single-branch external/libuv/
          git submodule update --init --recursive --single-branch external/uvwasi/
          git submodule update --init --recursive --single-branch external/wasm3/
          git submodule update --init --recursive --single-branch external/wasmer/
          git submodule update --init --recursive --single-branch platforms/iOS/external/SnapKit
          git submodule update --init --recursive --single-branch external/SDL/
          git submodule update --init --recursive --single-branch external/SDL_ttf/
        
      - name: Install packages
        run: |
          python3 -m venv .venv
          . .venv/bin/activate
          python3 -m pip install zstd
          
      - name: Install rust tools
        run: |
          cargo install cargo-lipo
          rustup target add aarch64-apple-ios
  
      - name: Cmake Cache
        uses: actions/cache@v4
        with:
          path: platforms/iOS/cmake_build/CMakeCache.txt
          key: ios-cmake-cache

      - name: Configure and build
        run: |
          . .venv/bin/activate
          sh platforms/iOS/build.sh --release

  build_pages:
    needs: [windows, web, linux, dos, android, macos, ios]
    permissions:
      pages: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Make _site directory
        run: |
          mkdir _site

      - uses: actions/download-artifact@v4
        with: 
          name: Artifacts-Linux
          path: _site

      - uses: actions/download-artifact@v4
        with: 
          name: Artifacts-Android
          path: _site

      - uses: actions/download-artifact@v4
        with: 
          name: Artifacts-Windows-Installer
          path: _site

      - uses: actions/download-artifact@v4
        with: 
          name: Artifacts-Windows-MSIX
          path: _site

      - uses: actions/download-artifact@v4
        with: 
          name: Artifacts-MacOS
          path: _site
          
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4

  update_pages:
    needs: build_pages
    env:
      GH_TOKEN: ${{secrets.PAGES_UPDATE_TOKEN}}
    runs-on: ubuntu-latest

    steps:
      - name: Ok
        run: |
          git clone https://github.com/webrogue-runtime/webrogue-runtime.github.io.git &&
          cd webrogue-runtime.github.io &&
          gh workflow run update_submodule
