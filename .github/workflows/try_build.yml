name: webrogue CI

on:
  push:
    
jobs:
  ci:
    uses: ./.github/workflows/reusable.yml
    with:
      is_release: false
    # secrets: inherit
  deploy:
    # if: ${{ github.ref_name == 'main' }}
    uses: ./.github/workflows/reusable.yml
    with:
      is_release: true
